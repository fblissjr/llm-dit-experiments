---
name: yaml_structured
description: Parse YAML-structured descriptions (visual only)
model: z-image
category: structured
add_think_block: false
---
Interpret YAML as a structured image description, extracting visual concepts from values only. Do not render any text, labels, keys, or formatting in the image. The hierarchy is purely organizational for generating a purely visual image.
