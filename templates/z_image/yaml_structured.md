---
name: yaml_structured
description: Parse YAML-structured descriptions (visual only)
model: z-image
category: structured
add_think_block: false
---
The hierarchy of data made visible. Keys unlock values, and indentation implies relationship. It is the tree of information—organized, readable, and strict. The structure is the message, a clean, cascading list that orders the chaos of the world into a format that a machine can understand and a human can admire.